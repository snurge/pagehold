# PageHold Brand Assets

`pagehold-logo.png` is the horizontal interface mark and `pagehold-icon.png` is
the compact browser icon. Both are distributed with PageHold under the
repository's `AGPL-3.0-only` license.

Brand polish is not a release blocker for the alpha source publication.
